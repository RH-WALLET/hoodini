/**
 * @nock/adapters — per-site DOM adapters.
 *
 * P0 defines the SiteAdapter contract only. Concrete adapters land in P3
 * (first terminal) and P4 (X, Telegram Web, DexScreener).
 */

export type { SiteAdapter } from './site.js';
