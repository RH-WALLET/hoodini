/**
 * Content script — placeholder.
 *
 * Registered so the extension's page surface exists and can be reviewed, but it
 * deliberately does nothing yet: no site adapters until P3, and no page-facing
 * capability exists in the protocol until P2c.
 *
 * When it does gain behaviour it will only ever REQUEST actions. It will never
 * hold a key, never sign, and never receive one — the service worker owns all
 * of that (ARCHITECTURE.md).
 */

export {};
